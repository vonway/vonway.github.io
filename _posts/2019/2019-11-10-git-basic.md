---
title: git基础操作
categories:
  - Blog
tags:
  - Git
description: git-basic
---

# git基础操作

## git configure
```
$ git config --global user.name "Your Name"
$ git config --global user.email "your.email@gmail.com"
$ git config --global core.editor <editor>    # <editor>: vi, vim, etc.

// 生成ssh的密钥
$ mkdir -p $HOME/.ssh
$ chmod 700 $HOME/.ssh
$ ssh-keygen -t rsa 
// 添加到gerrit中

```

## clone操作
```
git clone url
```

## todo
```
git branch
git pull
git init
git flow init
git diff
git show
git rebase
git reflog
git checkout
git fetch
git merge
git commit
git add
git push
git remote
git stash
git cherry-pick
git blame
git reset
git log
git tag
```


